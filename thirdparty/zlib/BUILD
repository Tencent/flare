# zlib via vcpkg (vcpkg#zlib:z). include_prefix exposes the headers as
# "zlib/<h>" (flare's include style). //thirdparty/zlib:z is unchanged.
cc_library(name='z', hdrs=[], deps=['vcpkg#zlib:z'], visibility='PUBLIC')
