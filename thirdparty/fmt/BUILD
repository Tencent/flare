# fmt is provided by vcpkg (vcpkg#fmt:fmt, pinned to 7.1.3 in BLADE_ROOT).
# This thin wrapper keeps the //thirdparty/fmt:fmt reference unchanged for
# every consumer.
cc_library(
    name = 'fmt',
    hdrs = [],
    deps = ['vcpkg#fmt:fmt'],
    visibility = 'PUBLIC',
)
