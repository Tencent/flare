# nghttp2 via vcpkg (vcpkg#nghttp2:nghttp2, baseline in BLADE_ROOT). flare
# includes "nghttp2/<h>" (vcpkg native include/nghttp2/ layout).
cc_library(
    name='nghttp2',
    hdrs=[],
    deps=['vcpkg#nghttp2:nghttp2'],
    visibility='PUBLIC',
)
